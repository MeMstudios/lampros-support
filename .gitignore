# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary, build with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIdE
*.out

#Creds
controllers/credentials.go
credentials.json
token.json
agents.json

# Main binary
lampros-support
